name: Test Good Candidate

# test comment

on:
  push:
    branches: [Main]
  pull_request:
    branches: [Main]

jobs:
  test-good:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          cd release_candidate_good
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Lint
        run: |
          cd release_candidate_good
          ruff check .

      - name: Type check
        run: |
          cd release_candidate_good
          mypy src

      - name: Tests
        run: |
          cd release_candidate_good
          pytest
